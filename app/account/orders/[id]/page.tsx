export default async function OrderDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;return <main className="shell py-12"><p className="eyebrow">Order {id}</p><h1 className="display mt-3 text-5xl">Order tracking</h1><div className="mt-10 grid gap-3 sm:grid-cols-4">{['Order placed','Payment confirmed','Preparing your order','Out for delivery'].map((x,i)=><div key={x} className={`p-5 ${i<2?'bg-navy text-white':'bg-white text-zinc-500'}`}><p className="eyebrow">0{i+1}</p><p className="mt-2 text-sm font-semibold">{x}</p></div>)}</div></main>}
