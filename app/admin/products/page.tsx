import Link from 'next/link'; import { getAllProducts } from '@/lib/data';
export default async function AdminProducts () { const products = await getAllProducts(); return <main className="shell py-12"><div className="flex items-center justify-between"><div><p className="eyebrow">Catalog</p><h1 className="display mt-2 text-5xl">Products</h1></div><Link className="button" href="/admin/products/new">Add product</Link></div><div className="mt-10 overflow-x-auto bg-white"><table className="w-full min-w-[680px] text-left text-sm"><thead className="border-b border-stone text-[10px] uppercase tracking-wider text-zinc-500"><tr><th className="p-4">Product</th><th>Category</th><th>Price</th><th>Stock</th><th>Status</th><th></th></tr></thead><tbody>{ products.map( p => <tr className="border-b border-stone" key={ p.id }><td className="p-4 font-semibold">{ p.name }<span className="block pt-1 text-xs font-normal text-zinc-500">{ p.slug }</span></td><td>{ p.category }</td><td>₹{ Number( p.price ).toLocaleString() }</td><td>{ p.stock }</td><td><span className="text-emerald-700">Active</span></td><td><Link className="text-xs underline" href={ `/admin/products/${ p.id }` }>Edit</Link></td></tr> ) }</tbody></table></div></main> }
