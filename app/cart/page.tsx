'use client';
import Image from 'next/image'; import Link from 'next/link'; import { useStore } from '@/components/StoreProvider';
const money=(n:number)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(n);
export default function Cart(){const {cart,removeFromCart,setQty}=useStore();const total=cart.reduce((a,x)=>a+x.product.price*x.quantity,0);if(!cart.length)return <main className="shell py-24 text-center"><h1 className="display text-5xl">Your bag is empty.</h1><p className="mt-4 text-zinc-600">Find something built for your everyday.</p><Link className="button mt-7" href="/shop">Continue shopping</Link></main>;return <main className="shell py-12"><h1 className="display text-5xl">Your bag</h1><div className="mt-10 grid gap-10 lg:grid-cols-[1fr_360px]"><div>{cart.map(item=><article className="flex gap-4 border-t border-stone py-5" key={item.product.id+item.size+item.color}><div className="relative h-32 w-24 shrink-0 bg-stone"><Image src={item.product.image} alt={item.product.name} fill className="object-cover" sizes="96px"/></div><div className="flex flex-1 flex-col"><div className="flex justify-between gap-3"><div><h2 className="font-semibold">{item.product.name}</h2><p className="mt-1 text-xs text-zinc-500">{item.color} · {item.size}</p></div><b>{money(item.product.price*item.quantity)}</b></div><div className="mt-auto flex items-center justify-between"><select aria-label="Quantity" className="border border-stone bg-white px-2 py-1 text-xs" value={item.quantity} onChange={e=>setQty(item.product.id,Number(e.target.value))}>{[1,2,3,4,5].map(x=><option key={x}>{x}</option>)}</select><button className="text-xs text-highland underline" onClick={()=>removeFromCart(item.product.id)}>Remove</button></div></div></article>)}</div><aside className="h-fit bg-white p-6"><h2 className="display text-2xl">Order summary</h2><div className="mt-6 space-y-3 border-b border-stone pb-5 text-sm"><div className="flex justify-between"><span>Subtotal</span><span>{money(total)}</span></div><div className="flex justify-between"><span>Shipping</span><span>{total>=4000?'Free':'₹250'}</span></div></div><div className="flex justify-between py-5 font-semibold"><span>Total</span><span>{money(total+(total>=4000?0:250))}</span></div><Link className="button w-full" href="/checkout">Secure checkout</Link></aside></div></main>}
